const helpers = require("./helpers.js");
const axios = require("axios");

module.exports = {
  sayGm: async function () {
    try {
    } catch (error) {
      console.log("error sending message");
      console.log(error.toString());
    }
  },
};
